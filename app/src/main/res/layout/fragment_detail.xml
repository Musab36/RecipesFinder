<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.salajim.musab.recipesfinder.DetailFragment">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="300dp"
            android:src="@drawable/recipes"
            android:scaleType="centerCrop"
            android:id="@+id/recipesImageView" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/recipesImageView"
            android:paddingTop="20dp"
            android:text="5 carrots, 1 onion, meat"
            android:textSize="20sp"
            android:textColor="@color/rating"
            android:textStyle="bold"
            android:id="@+id/ingredients" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/ingredients"
            android:paddingTop="20dp"
            android:text="View more info"
            android:textColor="@color/colorPrimary"
            android:textSize="20sp"
            android:textStyle="bold"
            android:drawableLeft="@drawable/ic_exit_to_app_black_24dp"
            android:drawablePadding="5dp"
            android:id="@+id/href" />

    </RelativeLayout>

</FrameLayout>
